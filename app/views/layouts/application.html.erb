<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title>JobHub</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application', "#{@layout_bundle || :application}-bundle" %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
