<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title>JobHub</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application', 'admin-bundle' %>
  </head>

  <body>
    <nav class='navbar navbar-light bg-light mb-3'>
      <%= link_to 'JobHub', admin_openings_path, class: 'navbar-brand' %>
    </nav>

    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
